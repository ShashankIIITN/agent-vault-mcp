[project]
name = "agent-vault-mcp"
version = "1.0.0"
description = "DSA-Optimized AI Knowledge Bank MCP Server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=2,<3",
]

[project.scripts]
agent-vault-mcp = "agent_vault.server:main"
agent-vault = "agent_vault.server:main"
